export type BlogCreateModel = {
    name: string,
    description: string,
    websiteUrl: string,
}

export type CreatePostInBlogModel = {
    title: string,
    shortDescription: string,
    content: string,
}